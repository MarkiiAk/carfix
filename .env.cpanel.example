# 🔐 Credenciales FTP para cPanel
# Copia este archivo y configura estos valores en GitHub Secrets

# ⚠️ NUNCA subas este archivo con valores reales a GitHub
# Solo úsalo como referencia

# ===========================================
# CONFIGURACIÓN FTP
# ===========================================

# Host del servidor FTP
# Ejemplo: ftp.tudominio.com o 192.168.1.1
FTP_SERVER=ftp.tudominio.com

# Usuario FTP de cPanel
# El que creaste en "Cuentas FTP" de tu cPanel
FTP_USERNAME=tu_usuario_ftp

# Contraseña del usuario FTP
# ⚠️ IMPORTANTE: Esta contraseña NUNCA debe estar en el código
# Solo se configura en GitHub Secrets
FTP_PASSWORD=tu_contraseña_super_segura

# Puerto FTP (opcional)
# Por defecto es 21, solo cámbialo si tu hosting usa otro
FTP_PORT=21

# Directorio destino en el servidor
# Para dominio principal: /public_html/
# Para subdominio: /public_html/subdominio/
# Para subcarpeta: /public_html/nombre-carpeta/
FTP_SERVER_DIR=/public_html/

# ===========================================
# NOTAS IMPORTANTES
# ===========================================

# 1. Estos valores NO se ponen en archivos .env
# 2. Se configuran en: GitHub → Settings → Secrets → Actions
# 3. Nunca compartas tu contraseña FTP
# 4. Si subes este archivo, asegúrate que esté en .gitignore

# ===========================================
# CÓMO OBTENER ESTOS VALORES
# ===========================================

# 1. Ingresa a tu cPanel (tudominio.com/cpanel)
# 2. Ve a "Archivos" → "Cuentas FTP"
# 3. Crea una cuenta FTP o usa una existente
# 4. Anota:
#    - Host FTP (FTP_SERVER)
#    - Usuario (FTP_USERNAME)
#    - Contraseña (FTP_PASSWORD)
#    - Puerto (FTP_PORT, normalmente 21)
# 5. Determina la ruta destino (FTP_SERVER_DIR)

# ===========================================
# PRUEBA TUS CREDENCIALES
# ===========================================

# Antes de configurar GitHub Actions, prueba tus credenciales con:
# - FileZilla (cliente FTP)
# - WinSCP (cliente FTP/SFTP)
# - Cyberduck (cliente FTP)

# Si no puedes conectarte con un cliente FTP,
# tampoco funcionará en GitHub Actions.

# ===========================================
# CONFIGURACIÓN EN GITHUB
# ===========================================

# Ve a: https://github.com/MarkiiAk/CotizadorTalleres
# 1. Settings
# 2. Secrets and variables → Actions
# 3. New repository secret
# 4. Agrega cada variable (FTP_SERVER, FTP_USERNAME, etc.)

# ===========================================
# SEGURIDAD
# ===========================================

# ✅ Las contraseñas están seguras en GitHub Secrets
# ✅ No se exponen en logs
# ✅ No se pueden ver después de crearlas
# ✅ Solo se usan durante el deploy
# ❌ NUNCA las pongas en código o archivos .env del proyecto
